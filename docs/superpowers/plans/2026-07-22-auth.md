# 인증 (익명 시작 · 매직링크 연결 · RLS 정책) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 익명 세션으로 즉시 기록할 수 있고, 나중에 매직링크로 같은 사용자 ID를 유지한 채 영구 계정이 되며, RLS가 교차 사용자 접근을 실제로 차단하는 상태를 만든다.

**Architecture:** 스펙(`docs/superpowers/specs/2026-07-22-auth-design.md`)의 결정을 그대로 옮긴다. 가장 위험한 가정(익명→영구 전환에서 ID 불변)을 **Task 1에서 자동화 테스트로 먼저 검증**하고, 그 뒤에야 그 가정에 의존하는 스키마·정책·코드를 쌓는다. RLS 정책은 소유자 직접과 부모 경유 `EXISTS` 두 형태만 쓴다.

**Tech Stack:** Supabase Auth · `@supabase/supabase-js` · `@supabase/ssr` · PostgreSQL RLS · Next.js 16 · Vitest(통합) · Mailpit

## Global Constraints

- 산출물은 **코드**다. `main` 직접 커밋 금지 — `feat/auth` 브랜치에서 작업한다(git.md).
- 커밋 메시지는 `<type>(<scope>): <한국어 요약>`. scope는 `auth` 또는 `db`. `Co-Authored-By` 트레일러를 남긴다.
- **마이그레이션은 up/down을 같은 커밋에.** down은 `supabase/migrations/down/<같은-타임스탬프>_<같은-이름>.down.sql`.
- 마이그레이션 타임스탬프는 `npx supabase migration new <name>` 생성값을 쓴다. 이 문서의 `<ts1>`~`<ts3>`를 그 값으로 대체한다.
- 로컬 Supabase 스택이 떠 있어야 한다(`npx supabase start`).
- 마이그레이션 **적용은 로컬까지만.** staging/production은 사람이 실행한다(CLAUDE.md 안전 가드).
- 인증·삭제 관련 변경이므로 병합 전 `/security-review`를 실행한다(privacy.md).
- **UI는 이 계획의 범위가 아니다.** 설정 화면·연결 안내 화면은 프론트 작업에서 만든다. 여기서는 서비스 계층까지만 낸다.
- 테스트의 로컬 접속 정보는 환경변수 우선, 없으면 Supabase 로컬 공개 기본값으로 대체한다. 기존 `SUPABASE_DB_URL` 패턴을 따른다.

## 스펙에서 조정한 범위

| 스펙 | 계획 | 이유 |
|---|---|---|
| §3 "설정 화면의 상시 진입점은 이번 범위에 포함" | **제외** | 화면을 만들면 frontend.md의 empty/loading/processing/error/offline 5종과 접근성 요구가 따라붙는다. 반쯤 만든 화면은 그 규칙을 어긴다. `linkAccount`는 서비스 계층 테스트로 검증하고, 화면은 프론트 작업에서 붙인다 |

---

## File Structure

| 경로 | 책임 |
|------|------|
| `supabase/config.toml` | 익명 로그인 활성화 |
| `supabase/migrations/<ts1>_auth_profile_link.sql` | `users.email` 제거 · `auth.users` FK · 프로필 트리거 |
| `supabase/migrations/<ts2>_rls_policies.sql` | RLS 정책 전체 |
| `lib/repositories/supabase.ts` | Supabase 클라이언트 생성(서버·브라우저·미들웨어) |
| `lib/services/session.ts` | `ensureSession()` · `linkAccount()` — 프레임워크 타입을 모름 |
| `middleware.ts` | 기존 세션 토큰 갱신만 |
| `app/auth/callback/route.ts` | PKCE `code` → 세션 교환 |
| `lib/repositories/authAssumptions.integration.test.ts` | 스펙 §9 가정 검증 |
| `lib/repositories/rls.integration.test.ts` | 교차 사용자 격리 |
| `lib/services/session.test.ts` | 서비스 단위 테스트(스텁) |
| `lib/repositories/testSupport.ts` | 테스트 공용 헬퍼(관리자 클라이언트·Mailpit·RLS 역할 전환) |

---

## Task 1: 익명 로그인 활성화와 ID 불변 검증

스펙 §9의 가정을 **가장 먼저** 검증한다. 여기서 ID가 바뀐다면 이후 설계 전체가 달라지므로 다른 작업을 시작하기 전에 확인한다.

**Files:**
- Modify: `supabase/config.toml:178`
- Create: `lib/repositories/testSupport.ts`, `lib/repositories/authAssumptions.integration.test.ts`
- Modify: `package.json` (의존성)

**Interfaces:**
- Consumes: 기존 `vitest.integration.config.mts`
- Produces: `adminClient()`, `anonClient()`, `latestMessageTo(email)`, `SUPABASE_URL`, `SERVICE_ROLE_KEY`, `ANON_KEY` — 이후 태스크의 테스트가 재사용한다

- [ ] **Step 1: 의존성 설치**

```powershell
npm install @supabase/supabase-js @supabase/ssr
```

- [ ] **Step 2: 익명 로그인 활성화**

`supabase/config.toml`의 178번째 줄을 바꾼다.

```toml
enable_anonymous_sign_ins = true
```

적용:

```powershell
npx supabase stop
npx supabase start
```

Expected: 재기동 후 서비스 URL 목록이 출력된다. `config.toml` 변경은 재기동해야 반영된다.

- [ ] **Step 3: 테스트 공용 헬퍼 작성**

`lib/repositories/testSupport.ts`:

```ts
import { createClient, type SupabaseClient } from "@supabase/supabase-js";

// Supabase 로컬 스택의 공개 기본값. 루프백 전용이며 비밀이 아니다.
// 원격을 대상으로 돌릴 때는 환경변수로 덮어쓴다.
export const SUPABASE_URL = process.env.SUPABASE_URL ?? "http://127.0.0.1:54321";
export const MAILPIT_URL = process.env.MAILPIT_URL ?? "http://127.0.0.1:54324";
export const SERVICE_ROLE_KEY =
  process.env.SUPABASE_SERVICE_ROLE_KEY ??
  "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImV4cCI6MTk4MzgxMjk5Nn0.EGIM96RAZx35lJzdJsyH-qQwv8Hdp7fsn3W0YpN81IU";
export const ANON_KEY =
  process.env.SUPABASE_ANON_KEY ??
  "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0";

/** RLS를 우회하는 관리자 클라이언트. 테스트 준비·정리에만 쓴다. */
export function adminClient(): SupabaseClient {
  return createClient(SUPABASE_URL, SERVICE_ROLE_KEY, {
    auth: { autoRefreshToken: false, persistSession: false },
  });
}

/** 실제 사용자와 같은 권한의 클라이언트. 세션마다 새로 만든다. */
export function anonClient(): SupabaseClient {
  return createClient(SUPABASE_URL, ANON_KEY, {
    auth: { autoRefreshToken: false, persistSession: false },
  });
}

type MailpitSummary = { ID: string; To: { Address: string }[] };
type MailpitMessage = { Text: string; HTML: string };

/** Mailpit에서 해당 주소로 온 가장 최근 메일의 본문을 가져온다. */
export async function latestMessageTo(address: string): Promise<string> {
  const list = await fetch(`${MAILPIT_URL}/api/v1/messages?limit=50`);
  if (!list.ok) throw new Error(`Mailpit 목록 조회 실패: ${list.status}`);
  const { messages } = (await list.json()) as { messages: MailpitSummary[] };

  const found = messages.find((m) =>
    m.To.some((to) => to.Address.toLowerCase() === address.toLowerCase()),
  );
  if (!found) throw new Error(`${address} 로 온 메일이 없다`);

  const detail = await fetch(`${MAILPIT_URL}/api/v1/message/${found.ID}`);
  if (!detail.ok) throw new Error(`Mailpit 본문 조회 실패: ${detail.status}`);
  const body = (await detail.json()) as MailpitMessage;
  return `${body.Text}\n${body.HTML}`;
}

/** 메일 본문에서 6자리 OTP를 뽑는다. 링크보다 파싱이 안정적이다. */
export function extractOtp(body: string): string {
  const match = body.match(/\b(\d{6})\b/);
  if (!match) throw new Error("본문에서 6자리 코드를 찾지 못했다");
  return match[1];
}

/** Mailpit 사서함을 비운다. 테스트 간 간섭을 막는다. */
export async function clearMailbox(): Promise<void> {
  await fetch(`${MAILPIT_URL}/api/v1/messages`, { method: "DELETE" });
}
```

**OTP를 쓰는 이유:** Supabase 확인 메일에는 링크와 6자리 코드가 함께 담긴다. 링크는 리다이렉션·인코딩 때문에 파싱이 깨지기 쉽지만 코드는 안정적이고 `verifyOtp`로 바로 검증할 수 있다.

- [ ] **Step 4: 가정 검증 테스트 작성**

`lib/repositories/authAssumptions.integration.test.ts`:

```ts
import { describe, it, expect, beforeEach } from "vitest";
import { adminClient, anonClient, latestMessageTo, extractOtp, clearMailbox } from "./testSupport";

beforeEach(async () => {
  await clearMailbox();
});

describe("스펙 §9 가정", () => {
  it("익명 로그인이 활성화되어 있다", async () => {
    const client = anonClient();
    const { data, error } = await client.auth.signInAnonymously();

    expect(error).toBeNull();
    expect(data.user?.id).toBeTruthy();
    expect(data.user?.is_anonymous).toBe(true);
  });

  it("이메일 연결 후에도 사용자 ID가 바뀌지 않는다", async () => {
    // 이 가정이 깨지면 데이터 이관 설계가 통째로 필요해진다.
    const client = anonClient();
    const { data: anon } = await client.auth.signInAnonymously();
    const anonymousId = anon.user!.id;

    const address = `link-${anonymousId.slice(0, 8)}@test.local`;
    const { error: updateError } = await client.auth.updateUser({ email: address });
    expect(updateError).toBeNull();

    const body = await latestMessageTo(address);
    const otp = extractOtp(body);

    const { data: verified, error: verifyError } = await client.auth.verifyOtp({
      email: address,
      token: otp,
      type: "email_change",
    });

    expect(verifyError).toBeNull();
    expect(verified.user!.id).toBe(anonymousId);
    expect(verified.user!.email).toBe(address);
    expect(verified.user!.is_anonymous).toBe(false);
  });

  it("전환 전에 남긴 데이터가 전환 후에도 같은 사용자 것이다", async () => {
    const client = anonClient();
    const { data: anon } = await client.auth.signInAnonymously();
    const anonymousId = anon.user!.id;

    const admin = adminClient();
    const { error: insertError } = await admin
      .from("memories")
      .insert({ user_id: anonymousId, source_type: "manual", memory_type: "moment" });
    expect(insertError).toBeNull();

    const address = `keep-${anonymousId.slice(0, 8)}@test.local`;
    await client.auth.updateUser({ email: address });
    const otp = extractOtp(await latestMessageTo(address));
    await client.auth.verifyOtp({ email: address, token: otp, type: "email_change" });

    const { data: rows } = await admin.from("memories").select("id").eq("user_id", anonymousId);
    expect(rows?.length).toBe(1);
  });
});
```

- [ ] **Step 5: 테스트 실행**

```powershell
npm run test:integration
```

Expected: PASS. 기존 10건 + 신규 3건 = **13 passed**.

**여기서 "이메일 연결 후에도 사용자 ID가 바뀌지 않는다"가 실패하면 중단한다.** 스펙의 전제가 틀린 것이므로 계획을 계속 실행하지 말고 사람에게 보고한다.

`double_confirm_changes = true`(`config.toml:224`) 때문에 확인 메일이 두 통 올 수 있다. 익명 사용자는 기존 이메일이 없어 한 통만 오는 것이 정상이지만, 두 통이 오면 `latestMessageTo`가 새 주소로 온 것을 고르므로 그대로 동작한다.

- [ ] **Step 6: 커밋**

```powershell
git add supabase/config.toml lib/repositories/testSupport.ts lib/repositories/authAssumptions.integration.test.ts package.json package-lock.json
git commit -m "test(auth): 익명 로그인 활성화와 ID 불변 가정 검증

스펙 §9의 가정을 자동화 테스트로 고정한다. 익명 사용자가
updateUser({email}) + OTP 확인 후에도 같은 id를 유지하는지 확인.
이 가정이 깨지면 데이터 이관 설계가 통째로 필요해지므로
스키마·정책보다 먼저 검증한다.

config.toml의 enable_anonymous_sign_ins가 기본 false였다."
```

---

## Task 2: 스키마 — email 제거 · auth FK · 프로필 트리거

**Files:**
- Create: `supabase/migrations/<ts1>_auth_profile_link.sql`, `supabase/migrations/down/<ts1>_auth_profile_link.down.sql`
- Modify: `lib/repositories/schema.integration.test.ts` (email 컬럼 사용 제거)

**Interfaces:**
- Consumes: 기존 `public.users` 테이블
- Produces: `public.users(id)` ← `auth.users(id)` FK, `public.handle_new_user()` 트리거 함수. 이후 모든 테스트에서 `users` 행은 **직접 만들지 않고** Auth 관리자 API로 사용자를 만들어 얻는다

- [ ] **Step 1: 마이그레이션 생성**

```powershell
npx supabase migration new auth_profile_link
```

- [ ] **Step 2: up 스크립트 작성**

`supabase/migrations/<ts1>_auth_profile_link.sql`:

```sql
-- public.users를 auth.users에 종속시키고 프로필을 자동 생성한다.

-- email은 auth.users가 단일 출처다. 여기에 복제하지 않는다:
-- 익명 사용자는 이메일이 없고, 소셜(카카오)은 이메일을 선택 동의로
-- 두며, 한 사용자가 여러 identity를 가지면 이메일이 둘 이상이 된다.
alter table public.users drop column email;

-- 인증과 프로필을 하나로 묶는다. 계정이 사라지면 프로필도 사라진다.
alter table public.users
  add constraint users_id_fkey
  foreign key (id) references auth.users(id) on delete cascade;

-- 프로필 생성을 앱이 아니라 DB에 둔다. 사용자 생성 경로가
-- 익명·매직링크·(후일)소셜로 늘어나는데, 한 경로라도 빠뜨리면
-- 프로필 없는 사용자가 생긴다. 트리거는 경로와 무관하게 걸린다.
create function public.handle_new_user() returns trigger
  language plpgsql
  security definer
  set search_path = ''
as $$
begin
  -- raw_user_meta_data를 처음부터 읽는다. 소셜 provider가 이름을
  -- 이 필드로 넘기므로, 이렇게 두면 소셜 추가 시 트리거를 안 고쳐도 된다.
  insert into public.users (id, display_name)
  values (new.id, new.raw_user_meta_data ->> 'name');
  return new;
end;
$$;

create trigger on_auth_user_created
  after insert on auth.users
  for each row execute function public.handle_new_user();
```

`search_path = ''`는 `security definer` 함수의 표준 방어다. 모든 객체를 스키마까지 명시했으므로 검색 경로 조작이 통하지 않는다.

- [ ] **Step 3: down 스크립트 작성**

`supabase/migrations/down/<ts1>_auth_profile_link.down.sql`:

```sql
drop trigger if exists on_auth_user_created on auth.users;
drop function if exists public.handle_new_user();
alter table public.users drop constraint if exists users_id_fkey;
-- email 복원은 데이터를 되살리지 못한다. 컬럼만 되돌린다.
alter table public.users add column email text;
```

**down이 완전 복원이 아님을 명시한다.** `drop column`은 데이터를 지우므로 되돌릴 수 없다. 이 마이그레이션은 실사용 데이터가 없는 시점에만 안전하며, 그것이 지금 실행하는 이유다.

- [ ] **Step 4: 기존 테스트에서 email 사용 제거**

`lib/repositories/schema.integration.test.ts`의 `createUser` 헬퍼가 `email`을 넣고 있다. Auth 관리자 API로 사용자를 만들도록 바꾼다.

파일 상단 import에 추가:

```ts
import { adminClient } from "./testSupport";
```

`createUser` 함수를 통째로 교체:

```ts
async function createUser(label: string): Promise<string> {
  // public.users는 트리거가 만든다. 직접 insert하면 FK 위반이다.
  const admin = adminClient();
  const { data, error } = await admin.auth.admin.createUser({
    email: `${label}-${Date.now()}@test.local`,
    email_confirm: true,
  });
  if (error) throw error;
  return data.user.id;
}
```

`beforeEach`의 정리도 Auth 사용자까지 지우도록 교체:

```ts
beforeEach(async () => {
  await db.query("delete from public.deletions");
  // auth.users를 지우면 CASCADE로 public.users와 그 하위가 따라 사라진다.
  const admin = adminClient();
  const { data } = await admin.auth.admin.listUsers({ perPage: 1000 });
  for (const user of data?.users ?? []) {
    await admin.auth.admin.deleteUser(user.id);
  }
});
```

- [ ] **Step 5: 프로필 자동 생성 테스트 추가**

`lib/repositories/schema.integration.test.ts` 끝에 추가:

```ts
describe("프로필 자동 생성", () => {
  it("auth 사용자가 생기면 public.users 행이 따라 생긴다", async () => {
    const userId = await createUser("profile");

    const rows = await db.query("select id from public.users where id = $1", [userId]);
    expect(rows.rowCount).toBe(1);
  });

  it("auth 사용자를 지우면 public.users 행도 사라진다", async () => {
    const userId = await createUser("cascade-auth");
    const admin = adminClient();

    await admin.auth.admin.deleteUser(userId);

    const rows = await db.query("select id from public.users where id = $1", [userId]);
    expect(rows.rowCount).toBe(0);
  });
});
```

- [ ] **Step 6: 적용 및 테스트**

```powershell
npx supabase db reset
npm run test:integration
```

Expected: 마이그레이션 6개가 순서대로 적용되고, 통합 테스트 **15 passed**(기존 10 + Task 1의 3 + 신규 2).

- [ ] **Step 7: 커밋**

```powershell
git add supabase/migrations lib/repositories/schema.integration.test.ts
git commit -m "feat(auth): users를 auth.users에 연결하고 프로필 트리거 추가

- public.users.email 제거. auth.users가 단일 출처다.
  익명은 이메일이 없고, 카카오는 이메일을 선택 동의로 두며,
  identity가 여럿이면 이메일도 여럿이 된다
- id에 auth.users FK(on delete cascade)
- 프로필 생성 트리거. 생성 경로가 늘어나도 누락이 불가능하다.
  raw_user_meta_data를 처음부터 읽어 소셜 추가 시 수정이 필요 없다

기존 테스트의 createUser를 Auth 관리자 API 경유로 교체했다.
FK가 생겨 public.users 직접 insert가 더는 불가능하다."
```

---

## Task 3: RLS 정책과 교차 사용자 격리

`testing.md`의 필수 회귀 시나리오 "타 사용자 데이터가 절대 안 섞임"을 정책과 테스트로 동시에 만든다.

**Files:**
- Create: `supabase/migrations/<ts2>_rls_policies.sql`, `supabase/migrations/down/<ts2>_rls_policies.down.sql`, `lib/repositories/rls.integration.test.ts`

**Interfaces:**
- Consumes: Task 2의 프로필 트리거, Task 1의 `adminClient()`
- Produces: 없음 (정책은 DB에만 존재)

- [ ] **Step 1: 마이그레이션 생성**

```powershell
npx supabase migration new rls_policies
```

- [ ] **Step 2: up 스크립트 작성**

`supabase/migrations/<ts2>_rls_policies.sql`:

```sql
-- RLS 정책. 형태는 둘뿐이다 — 소유자 직접, 부모 경유 EXISTS.
-- 형태를 늘리지 않는 것이 검토 가능성을 지킨다.
--
-- (select auth.uid())로 감싸는 것은 Postgres가 행마다 재평가하지
-- 않고 한 번만 계산하게 하기 위함이다.

-- ── 소유자 직접 ────────────────────────────────────────────────
create policy "본인 프로필만" on public.users
  for all to authenticated
  using (id = (select auth.uid()))
  with check (id = (select auth.uid()));

create policy "본인 데이터만" on public.memories
  for all to authenticated
  using (user_id = (select auth.uid()))
  with check (user_id = (select auth.uid()));

create policy "본인 데이터만" on public.differences
  for all to authenticated
  using (user_id = (select auth.uid()))
  with check (user_id = (select auth.uid()));

create policy "본인 데이터만" on public.diaries
  for all to authenticated
  using (user_id = (select auth.uid()))
  with check (user_id = (select auth.uid()));

create policy "본인 데이터만" on public.entities
  for all to authenticated
  using (user_id = (select auth.uid()))
  with check (user_id = (select auth.uid()));

create policy "본인 데이터만" on public.signals
  for all to authenticated
  using (user_id = (select auth.uid()))
  with check (user_id = (select auth.uid()));

create policy "본인 데이터만" on public.baselines
  for all to authenticated
  using (user_id = (select auth.uid()))
  with check (user_id = (select auth.uid()));

create policy "본인 데이터만" on public.weekly_reports
  for all to authenticated
  using (user_id = (select auth.uid()))
  with check (user_id = (select auth.uid()));

create policy "본인 데이터만" on public.consents
  for all to authenticated
  using (user_id = (select auth.uid()))
  with check (user_id = (select auth.uid()));

-- ── 부모 경유 EXISTS ───────────────────────────────────────────
-- 소유자가 부모 한 곳에만 존재하므로 부모와 자식의 소유자가
-- 어긋나는 상황이 원천적으로 불가능하다.

create policy "부모 소유자만" on public.assets
  for all to authenticated
  using (exists (
    select 1 from public.memories m
     where m.id = memory_id and m.user_id = (select auth.uid())
  ));

create policy "부모 소유자만" on public.emotions
  for all to authenticated
  using (exists (
    select 1 from public.memories m
     where m.id = memory_id and m.user_id = (select auth.uid())
  ));

create policy "부모 소유자만" on public.memory_entities
  for all to authenticated
  using (exists (
    select 1 from public.memories m
     where m.id = memory_id and m.user_id = (select auth.uid())
  ));

create policy "부모 소유자만" on public.relations
  for all to authenticated
  using (exists (
    select 1 from public.entities e
     where e.id = source_entity_id and e.user_id = (select auth.uid())
  ));

create policy "부모 소유자만" on public.difference_evidence
  for all to authenticated
  using (exists (
    select 1 from public.differences d
     where d.id = difference_id and d.user_id = (select auth.uid())
  ));

create policy "부모 소유자만" on public.diary_sources
  for all to authenticated
  using (exists (
    select 1 from public.diaries di
     where di.id = diary_id and di.user_id = (select auth.uid())
  ));

create policy "부모 소유자만" on public.diary_sections
  for all to authenticated
  using (exists (
    select 1 from public.diaries di
     where di.id = diary_id and di.user_id = (select auth.uid())
  ));

create policy "부모 소유자만" on public.weekly_report_highlights
  for all to authenticated
  using (exists (
    select 1 from public.weekly_reports w
     where w.id = report_id and w.user_id = (select auth.uid())
  ));

-- ── deletions — 읽기 전용 ──────────────────────────────────────
-- insert/update/delete 정책을 만들지 않는다. 정책이 없으면 차단된다.
-- 사용자가 steps_done을 위조하면 실제로 지워지지 않은 단계가
-- 완료로 표시되어 삭제 완전성이 무너진다.
-- 쓰기는 Route Handler와 워커가 service_role로 수행한다.
create policy "본인 삭제 진행만 조회" on public.deletions
  for select to authenticated
  using (user_id = (select auth.uid()));
```

- [ ] **Step 3: down 스크립트 작성**

`supabase/migrations/down/<ts2>_rls_policies.down.sql`:

```sql
-- 정책만 제거한다. RLS 활성화는 유지되므로 되돌린 뒤에도
-- authenticated는 아무것도 볼 수 없다(안전 방향으로 실패).
drop policy if exists "본인 삭제 진행만 조회" on public.deletions;
drop policy if exists "부모 소유자만" on public.weekly_report_highlights;
drop policy if exists "부모 소유자만" on public.diary_sections;
drop policy if exists "부모 소유자만" on public.diary_sources;
drop policy if exists "부모 소유자만" on public.difference_evidence;
drop policy if exists "부모 소유자만" on public.relations;
drop policy if exists "부모 소유자만" on public.memory_entities;
drop policy if exists "부모 소유자만" on public.emotions;
drop policy if exists "부모 소유자만" on public.assets;
drop policy if exists "본인 데이터만" on public.consents;
drop policy if exists "본인 데이터만" on public.weekly_reports;
drop policy if exists "본인 데이터만" on public.baselines;
drop policy if exists "본인 데이터만" on public.signals;
drop policy if exists "본인 데이터만" on public.entities;
drop policy if exists "본인 데이터만" on public.diaries;
drop policy if exists "본인 데이터만" on public.differences;
drop policy if exists "본인 데이터만" on public.memories;
drop policy if exists "본인 프로필만" on public.users;
```

- [ ] **Step 4: 격리 테스트 작성**

`lib/repositories/rls.integration.test.ts`:

```ts
import { describe, it, expect, beforeAll, afterAll } from "vitest";
import { adminClient } from "./testSupport";
import type { SupabaseClient } from "@supabase/supabase-js";

let admin: SupabaseClient;
let alice: string;
let bob: string;
let aliceMemory: string;
let aliceDifference: string;
let aliceDiary: string;

/**
 * 해당 이메일의 사용자로 로그인한 클라이언트를 만든다. 실제 RLS를 통과한다.
 * 사용자 ID가 아니라 이메일을 받는다 — generateLink가 이메일로 사용자를 찾는다.
 */
async function clientFor(email: string): Promise<SupabaseClient> {
  const { createClient } = await import("@supabase/supabase-js");
  const { SUPABASE_URL, ANON_KEY } = await import("./testSupport");
  const { data, error } = await admin.auth.admin.generateLink({
    type: "magiclink",
    email,
  });
  if (error) throw error;

  const client = createClient(SUPABASE_URL, ANON_KEY, {
    auth: { autoRefreshToken: false, persistSession: false },
  });
  const { error: verifyError } = await client.auth.verifyOtp({
    token_hash: data.properties.hashed_token,
    type: "magiclink",
  });
  if (verifyError) throw verifyError;
  return client;
}

beforeAll(async () => {
  admin = adminClient();

  const { data: a } = await admin.auth.admin.createUser({
    email: "alice@test.local",
    email_confirm: true,
  });
  const { data: b } = await admin.auth.admin.createUser({
    email: "bob@test.local",
    email_confirm: true,
  });
  alice = a.user!.id;
  bob = b.user!.id;

  const { data: mem } = await admin
    .from("memories")
    .insert({ user_id: alice, source_type: "manual", memory_type: "moment" })
    .select("id")
    .single();
  aliceMemory = mem!.id;

  const { data: diff } = await admin
    .from("differences")
    .insert({
      user_id: alice,
      date: "2026-03-01",
      dimension: "퇴근시간",
      detection_method: "zscore",
      category: "오늘의다른점",
    })
    .select("id")
    .single();
  aliceDifference = diff!.id;

  const { data: diary } = await admin
    .from("diaries")
    .insert({ user_id: alice, date: "2026-03-01" })
    .select("id")
    .single();
  aliceDiary = diary!.id;

  await admin.from("difference_evidence").insert({
    difference_id: aliceDifference,
    memory_id: aliceMemory,
  });
  await admin.from("diary_sources").insert({
    diary_id: aliceDiary,
    memory_id: aliceMemory,
  });
});

afterAll(async () => {
  await admin.auth.admin.deleteUser(alice);
  await admin.auth.admin.deleteUser(bob);
});

describe("교차 사용자 격리 — 소유자 직접", () => {
  it("Bob은 Alice의 메모를 볼 수 없다", async () => {
    const bobClient = await clientFor("bob@test.local");
    const { data } = await bobClient.from("memories").select("id");
    expect(data).toEqual([]);
  });

  it("Bob은 Alice의 일기를 볼 수 없다", async () => {
    const bobClient = await clientFor("bob@test.local");
    const { data } = await bobClient.from("diaries").select("id");
    expect(data).toEqual([]);
  });

  it("Alice는 자기 메모를 볼 수 있다", async () => {
    const aliceClient = await clientFor("alice@test.local");
    const { data } = await aliceClient.from("memories").select("id");
    expect(data?.map((r) => r.id)).toEqual([aliceMemory]);
  });

  it("Bob은 Alice 소유로 메모를 만들 수 없다", async () => {
    const bobClient = await clientFor("bob@test.local");
    const { error } = await bobClient
      .from("memories")
      .insert({ user_id: alice, source_type: "manual", memory_type: "moment" });
    expect(error).not.toBeNull();
  });
});

describe("교차 사용자 격리 — 부모 경유", () => {
  it("Bob은 Alice의 근거 링크를 볼 수 없다", async () => {
    const bobClient = await clientFor("bob@test.local");
    const { data } = await bobClient.from("difference_evidence").select("difference_id");
    expect(data).toEqual([]);
  });

  it("Bob은 Alice의 일기 출처를 볼 수 없다", async () => {
    const bobClient = await clientFor("bob@test.local");
    const { data } = await bobClient.from("diary_sources").select("diary_id");
    expect(data).toEqual([]);
  });

  it("Alice는 자기 근거 링크를 볼 수 있다", async () => {
    const aliceClient = await clientFor("alice@test.local");
    const { data } = await aliceClient.from("difference_evidence").select("difference_id");
    expect(data?.length).toBe(1);
  });
});

describe("deletions 쓰기 차단", () => {
  it("사용자는 ledger에 행을 만들 수 없다", async () => {
    const aliceClient = await clientFor("alice@test.local");
    const { error } = await aliceClient
      .from("deletions")
      .insert({ user_id: alice, trigger: "memory", target_type: "memory" });
    expect(error).not.toBeNull();
  });

  it("사용자는 자기 ledger를 조회할 수는 있다", async () => {
    await admin
      .from("deletions")
      .insert({ user_id: alice, trigger: "memory", target_type: "memory" });

    const aliceClient = await clientFor("alice@test.local");
    const { data, error } = await aliceClient.from("deletions").select("id");
    expect(error).toBeNull();
    expect(data?.length).toBe(1);
  });
});
```

- [ ] **Step 5: 적용 및 테스트**

```powershell
npx supabase db reset
npm run test:integration
```

Expected: **24 passed** (기존 15 + 신규 9).

`generateLink`가 실패하면 그 사용자의 이메일이 `createUser`에서 쓴 것과 달라서다. `clientFor`가 만드는 주소(`<id앞8자>@test.local`)와 `beforeAll`의 주소가 다르므로, **`clientFor`를 사용자 생성 시 쓴 이메일을 받도록 고친다**:

```ts
async function clientFor(email: string): Promise<SupabaseClient> { /* email 로 generateLink */ }
```

호출부도 `clientFor("alice@test.local")` / `clientFor("bob@test.local")`로 바꾼다.

- [ ] **Step 6: 뮤테이션 점검**

정책이 실제로 막는지 확인한다. 정책 하나를 지우고 격리 테스트가 깨지는지 본다.

```powershell
node -e "const{Client}=require('pg');const c=new Client({connectionString:'postgresql://postgres:postgres@127.0.0.1:54322/postgres'});c.connect().then(()=>c.query('drop policy \"본인 데이터만\" on public.memories')).then(()=>{console.log('policy dropped');return c.end()})"
npm run test:integration
```

Expected: FAIL — "Bob은 Alice의 메모를 볼 수 없다"가 깨진다. 정책이 사라지면 `authenticated`는 아무것도 못 보므로 "Alice는 자기 메모를 볼 수 있다"도 함께 깨진다. **두 방향 모두 깨지는 것이 정상이다.**

복구:

```powershell
npx supabase db reset
npm run test:integration
```

Expected: PASS, 24 passed.

- [ ] **Step 7: 커밋**

```powershell
git add supabase/migrations lib/repositories/rls.integration.test.ts
git commit -m "feat(auth): RLS 정책과 교차 사용자 격리 테스트

정책 형태를 둘로 제한한다 — 소유자 직접, 부모 경유 EXISTS.
형태를 늘리지 않는 것이 검토 가능성을 지킨다.

deletions는 select 정책만 만든다. 사용자가 steps_done을 위조하면
지워지지 않은 단계가 완료로 표시되어 삭제 완전성이 무너진다.

testing.md 필수 시나리오 '타 사용자 데이터가 절대 안 섞임'을
실제 로그인 세션으로 검증한다. 뮤테이션 점검으로 정책이
실제 방어선인지 확인했다."
```

---

## Task 4: 저장소 계층 — Supabase 클라이언트

**Files:**
- Create: `lib/repositories/supabase.ts`

**Interfaces:**
- Consumes: `@supabase/ssr`
- Produces: `createServerSupabase()`, `createBrowserSupabase()`, `createMiddlewareSupabase(request, response)`

- [ ] **Step 1: Next 16의 쿠키 API 확인**

`@supabase/ssr`은 쿠키 읽기/쓰기 함수를 주입받는다. Next 16에서 `cookies()`가 비동기인지 확인한다.

```powershell
node -e "console.log(require('fs').readFileSync('node_modules/next/dist/docs/01-app/03-api-reference/04-functions/cookies.md','utf8').slice(0,3000))"
```

`cookies()`가 `Promise`를 반환하면 아래 코드의 `await`를 유지하고, 동기라면 `await`를 뺀다. **문서를 먼저 읽고 코드를 맞춘다**(AGENTS.md).

- [ ] **Step 2: 클라이언트 생성 모듈 작성**

`lib/repositories/supabase.ts`:

```ts
import { createServerClient, createBrowserClient } from "@supabase/ssr";
import { cookies } from "next/headers";
import type { NextRequest, NextResponse } from "next/server";

const SUPABASE_URL = process.env.NEXT_PUBLIC_SUPABASE_URL!;
const SUPABASE_ANON_KEY = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!;

/** 서버 컴포넌트·Route Handler용. 요청마다 새로 만든다. */
export async function createServerSupabase() {
  const cookieStore = await cookies();
  return createServerClient(SUPABASE_URL, SUPABASE_ANON_KEY, {
    cookies: {
      getAll: () => cookieStore.getAll(),
      setAll: (list) => {
        for (const { name, value, options } of list) {
          cookieStore.set(name, value, options);
        }
      },
    },
  });
}

/** 클라이언트 컴포넌트용. */
export function createBrowserSupabase() {
  return createBrowserClient(SUPABASE_URL, SUPABASE_ANON_KEY);
}

/** 미들웨어용. 갱신된 토큰을 응답 쿠키에 반영한다. */
export function createMiddlewareSupabase(request: NextRequest, response: NextResponse) {
  return createServerClient(SUPABASE_URL, SUPABASE_ANON_KEY, {
    cookies: {
      getAll: () => request.cookies.getAll(),
      setAll: (list) => {
        for (const { name, value, options } of list) {
          response.cookies.set(name, value, options);
        }
      },
    },
  });
}
```

서버 컴포넌트에서 `cookieStore.set`은 던질 수 있다. Route Handler와 미들웨어에서만 쓰기가 허용되며, 세션 갱신은 미들웨어가 담당하므로 정상 동작이다.

- [ ] **Step 3: 환경변수 파일 작성**

`.env.local` (`.gitignore`의 `.env.*`에 걸려 커밋되지 않는다):

```
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0
```

`.env.example`(커밋함):

```
# Supabase 로컬 스택 기본값. `npx supabase status`로 확인할 수 있다.
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=
```

- [ ] **Step 4: 타입·린트 확인**

```powershell
npm run check
```

Expected: 종료 코드 0. `lib/repositories/`는 서비스 계층이 아니므로 `next/*` import가 허용된다(ESLint 규칙은 `lib/services/**`에만 걸린다).

- [ ] **Step 5: 커밋**

```powershell
git add lib/repositories/supabase.ts .env.example
git commit -m "feat(auth): Supabase 클라이언트 저장소 계층

서버·브라우저·미들웨어 클라이언트 생성을 저장소 계층에 숨긴다.
서비스 계층이 @supabase/ssr을 직접 알지 않게 한다."
```

---

## Task 5: 서비스 계층 — ensureSession · linkAccount

**Files:**
- Create: `lib/services/session.ts`, `lib/services/session.test.ts`

**Interfaces:**
- Consumes: Task 4의 클라이언트 (인터페이스로 주입받는다)
- Produces: `ensureSession(auth: AuthPort): Promise<SessionResult>`, `linkAccount(auth: AuthPort, method: LinkMethod): Promise<LinkResult>`

- [ ] **Step 1: 실패하는 테스트 작성**

`lib/services/session.test.ts`:

```ts
import { describe, it, expect, vi } from "vitest";
import { ensureSession, linkAccount, type AuthPort } from "./session";

function stubAuth(overrides: Partial<AuthPort> = {}): AuthPort {
  return {
    getUser: vi.fn().mockResolvedValue(null),
    signInAnonymously: vi.fn().mockResolvedValue({ id: "anon-1", isAnonymous: true }),
    updateEmail: vi.fn().mockResolvedValue(undefined),
    signInWithOtp: vi.fn().mockResolvedValue(undefined),
    ...overrides,
  };
}

describe("ensureSession", () => {
  it("세션이 없으면 익명 세션을 만든다", async () => {
    const auth = stubAuth();

    const result = await ensureSession(auth);

    expect(auth.signInAnonymously).toHaveBeenCalledOnce();
    expect(result).toEqual({ userId: "anon-1", isAnonymous: true, created: true });
  });

  it("세션이 있으면 새로 만들지 않는다", async () => {
    const auth = stubAuth({
      getUser: vi.fn().mockResolvedValue({ id: "existing", isAnonymous: false }),
    });

    const result = await ensureSession(auth);

    expect(auth.signInAnonymously).not.toHaveBeenCalled();
    expect(result).toEqual({ userId: "existing", isAnonymous: false, created: false });
  });
});

describe("linkAccount", () => {
  it("익명 사용자에게 이메일을 붙인다", async () => {
    const auth = stubAuth({
      getUser: vi.fn().mockResolvedValue({ id: "anon-1", isAnonymous: true }),
    });

    const result = await linkAccount(auth, { kind: "email", address: "me@example.com" });

    expect(auth.updateEmail).toHaveBeenCalledWith("me@example.com");
    expect(result).toEqual({ status: "confirmation_sent" });
  });

  it("이미 영구 계정이면 다시 연결하지 않는다", async () => {
    const auth = stubAuth({
      getUser: vi.fn().mockResolvedValue({ id: "user-1", isAnonymous: false }),
    });

    const result = await linkAccount(auth, { kind: "email", address: "me@example.com" });

    expect(auth.updateEmail).not.toHaveBeenCalled();
    expect(result).toEqual({ status: "already_linked" });
  });

  it("세션이 없으면 로그인으로 처리한다", async () => {
    const auth = stubAuth();

    const result = await linkAccount(auth, { kind: "email", address: "me@example.com" });

    expect(auth.signInWithOtp).toHaveBeenCalledWith("me@example.com");
    expect(auth.updateEmail).not.toHaveBeenCalled();
    expect(result).toEqual({ status: "confirmation_sent" });
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

```powershell
npm test
```

Expected: FAIL — `Cannot find module './session'`.

- [ ] **Step 3: 구현 작성**

`lib/services/session.ts`:

```ts
/**
 * 세션 규칙. 프레임워크·HTTP·Supabase 타입을 모른다(backend.md).
 * 저장소 계층이 AuthPort를 구현해 주입한다.
 */

export type SessionUser = { id: string; isAnonymous: boolean };

export interface AuthPort {
  getUser(): Promise<SessionUser | null>;
  signInAnonymously(): Promise<SessionUser>;
  updateEmail(address: string): Promise<void>;
  signInWithOtp(address: string): Promise<void>;
}

export type SessionResult = {
  userId: string;
  isAnonymous: boolean;
  /** 이 호출이 세션을 새로 만들었는지 */
  created: boolean;
};

/** 이메일 외 수단은 여기에 분기를 추가한다(예: { kind: 'oauth', provider: 'kakao' }). */
export type LinkMethod = { kind: "email"; address: string };

export type LinkResult = { status: "confirmation_sent" | "already_linked" };

/**
 * 세션을 보장한다.
 *
 * 기록을 남기려는 시점에만 호출한다. 미들웨어처럼 모든 요청에 걸리는
 * 곳에서 부르면 크롤러가 auth.users를 유령 계정으로 채운다.
 */
export async function ensureSession(auth: AuthPort): Promise<SessionResult> {
  const existing = await auth.getUser();
  if (existing) {
    return { userId: existing.id, isAnonymous: existing.isAnonymous, created: false };
  }

  const created = await auth.signInAnonymously();
  return { userId: created.id, isAnonymous: created.isAnonymous, created: true };
}

/**
 * 계정을 연결한다. 수단이 무엇이든 "identity를 연결한다"는 같은 동작이므로
 * 여기 한 곳으로 모은다. 호출부가 수단별로 갈라지지 않게 한다.
 */
export async function linkAccount(auth: AuthPort, method: LinkMethod): Promise<LinkResult> {
  const user = await auth.getUser();

  if (user && !user.isAnonymous) {
    return { status: "already_linked" };
  }

  if (user) {
    // 익명 사용자에게 이메일을 붙인다. 사용자 ID는 그대로다.
    await auth.updateEmail(method.address);
  } else {
    // 세션이 없다 — 새 기기에서의 로그인이다.
    await auth.signInWithOtp(method.address);
  }

  return { status: "confirmation_sent" };
}
```

- [ ] **Step 4: 테스트 통과 확인**

```powershell
npm test
```

Expected: PASS. 기존 7 + 신규 7 = **14 passed**.

- [ ] **Step 5: 계층 규칙 확인**

서비스 계층이 프레임워크를 모르는지 lint로 확인한다.

```powershell
npx eslint lib/services
```

Expected: 종료 코드 0. `session.ts`가 `next/*`를 import하면 Task 3(스캐폴딩)에서 만든 규칙이 막는다.

- [ ] **Step 6: 커밋**

```powershell
git add lib/services/session.ts lib/services/session.test.ts
git commit -m "feat(auth): 세션 서비스 — ensureSession·linkAccount

AuthPort 인터페이스로 Supabase를 가려 서비스 계층이 프레임워크를
모르게 한다(backend.md 역방향 의존 금지).

linkAccount를 단일 진입점으로 둔다. 익명→영구 전환은 수단이
무엇이든 같은 동작이므로, 소셜 추가 시 LinkMethod에 분기만 넣는다."
```

---

## Task 6: 경계 — 미들웨어와 콜백 라우트

**Files:**
- Create: `middleware.ts`, `app/auth/callback/route.ts`

**Interfaces:**
- Consumes: Task 4의 `createMiddlewareSupabase`, `createServerSupabase`
- Produces: 없음 (경계는 외부에서만 호출된다)

- [ ] **Step 1: 미들웨어 작성**

`middleware.ts`:

```ts
import { NextResponse, type NextRequest } from "next/server";
import { createMiddlewareSupabase } from "@/lib/repositories/supabase";

export async function middleware(request: NextRequest) {
  const response = NextResponse.next({ request });
  const supabase = createMiddlewareSupabase(request, response);

  // 기존 세션 토큰 갱신만 한다. 세션이 없어도 만들지 않는다.
  // 미들웨어는 모든 요청에 걸리므로, 여기서 익명 세션을 만들면
  // 크롤러·프리페치가 auth.users를 유령 계정으로 채운다.
  await supabase.auth.getUser();

  return response;
}

export const config = {
  matcher: [
    // 정적 자산과 이미지 최적화 요청은 건드리지 않는다.
    "/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)",
  ],
};
```

- [ ] **Step 2: 콜백 라우트 작성**

`app/auth/callback/route.ts`:

```ts
import { NextResponse, type NextRequest } from "next/server";
import { createServerSupabase } from "@/lib/repositories/supabase";

/**
 * PKCE code를 세션으로 교환한다.
 *
 * 이 라우트는 provider를 모른다. 매직링크도 OAuth도 같은 교환을
 * 거치므로, 소셜을 추가해도 이 파일은 바뀌지 않는다.
 */
export async function GET(request: NextRequest) {
  const { searchParams, origin } = new URL(request.url);
  const code = searchParams.get("code");
  const next = searchParams.get("next") ?? "/";

  if (!code) {
    return NextResponse.redirect(`${origin}/auth/error?reason=missing_code`);
  }

  const supabase = await createServerSupabase();
  const { error } = await supabase.auth.exchangeCodeForSession(code);

  if (error) {
    // 실패를 조용히 삼키지 않는다(frontend.md). 사유는 코드로만 넘기고
    // 본문·이메일 등 식별 정보는 URL에 싣지 않는다.
    return NextResponse.redirect(`${origin}/auth/error?reason=exchange_failed`);
  }

  // 오픈 리다이렉트 방지: 같은 출처의 경로만 허용한다.
  const target = next.startsWith("/") && !next.startsWith("//") ? next : "/";
  return NextResponse.redirect(`${origin}${target}`);
}
```

- [ ] **Step 3: 빌드와 검사**

```powershell
npm run build
npm run check
```

Expected: 둘 다 종료 코드 0. 빌드 출력의 라우트 표에 `/auth/callback`이 나타난다.

- [ ] **Step 4: 미들웨어가 계정을 만들지 않는지 확인**

```powershell
node -e "const{Client}=require('pg');const c=new Client({connectionString:'postgresql://postgres:postgres@127.0.0.1:54322/postgres'});c.connect().then(()=>c.query('select count(*)::int as n from auth.users')).then(r=>{console.log('before',r.rows[0].n);return c.end()})"
```

개발 서버를 별도 창에서 띄운다(PowerShell에는 `&` 백그라운드 연산자가 없다).

```powershell
npm run build
$server = Start-Process -FilePath "npx" -ArgumentList "next","start" -PassThru
Start-Sleep -Seconds 5
1..3 | ForEach-Object { Invoke-WebRequest -Uri "http://localhost:3000/" -UseBasicParsing | Out-Null }
node -e "const{Client}=require('pg');const c=new Client({connectionString:'postgresql://postgres:postgres@127.0.0.1:54322/postgres'});c.connect().then(()=>c.query('select count(*)::int as n from auth.users')).then(r=>{console.log('after',r.rows[0].n);return c.end()})"
Stop-Process -Id $server.Id -Force
```

Expected: `before`와 `after`가 **같다.** 늘어났다면 미들웨어가 세션을 만들고 있는 것이므로 고친다.

- [ ] **Step 5: 커밋**

```powershell
git add middleware.ts app/auth/callback/route.ts
git commit -m "feat(auth): 미들웨어와 PKCE 콜백 라우트

미들웨어는 기존 세션 갱신만 한다. 모든 요청에 걸리므로 여기서
익명 세션을 만들면 크롤러가 auth.users를 유령 계정으로 채운다.

콜백은 provider를 모른다. 매직링크도 OAuth도 같은 code 교환을
거치므로 소셜 추가 시 이 파일은 바뀌지 않는다.
next 파라미터는 같은 출처 경로만 허용해 오픈 리다이렉트를 막는다."
```

---

## Task 7: 문서 갱신

**Files:**
- Modify: `README.md`, `supabase/README.md`, `docs/design/ERD.mermaid`

**Interfaces:**
- Consumes: Task 1–6의 결과
- Produces: 없음

- [ ] **Step 1: ERD에서 email 제거**

`docs/design/ERD.mermaid`의 `USERS` 블록에서 `text email` 줄을 지우고 주석을 남긴다.

```
    USERS {
        uuid id PK "auth.users(id) 참조"
        text display_name
        jsonb style_profile "기본 톤 프리셋"
        time diary_time "일기 생성 시각"
        text timezone "하루 경계 기준(backend.md)"
        timestamptz created_at
    }
```

- [ ] **Step 2: README에 환경변수 안내 추가**

`README.md`의 "### 2. 로컬 개발" 절 `npm install` 다음에 추가한다.

```markdown
`.env.local`을 만든다(`.env.example` 참고). 값은 `npx supabase status`로 확인한다.
```

- [ ] **Step 3: supabase/README에 인증 메모 추가**

`supabase/README.md` 끝에 추가한다.

```markdown
## 인증

- 익명 로그인이 켜져 있다(`config.toml`의 `enable_anonymous_sign_ins`).
- 메일은 Mailpit(`http://127.0.0.1:54324`)으로 간다. 실제 발송되지 않는다.
- RLS 정책은 소유자 직접과 부모 경유 EXISTS 두 형태뿐이다. 새 테이블을
  추가하면 둘 중 하나를 골라 정책을 함께 넣는다. 정책 없이 RLS만 켜면
  그 테이블은 앱에서 전혀 보이지 않는다.
```

- [ ] **Step 4: 전체 검사**

```powershell
npm run check
npm run test:integration
npx supabase db reset
```

Expected: 모두 종료 코드 0.

- [ ] **Step 5: 커밋**

```powershell
git add README.md supabase/README.md docs/design/ERD.mermaid
git commit -m "docs: 인증 반영 — ERD의 email 제거와 환경변수 안내"
```

- [ ] **Step 6: 보안 리뷰**

인증·RLS 변경이므로 `privacy.md`가 `/security-review`를 요구한다. 실행하고 결과를 확인한 뒤 병합으로 넘어간다.

- [ ] **Step 7: 브랜치 마무리**

`/superpowers:finishing-a-development-branch`. git.md에 따라 rebase 후 `merge --no-ff`, squash 금지.

---

## 완료 기준

- 익명 사용자가 이메일 연결 후에도 **같은 ID를 유지**한다(Task 1 테스트)
- `auth.users` 생성 시 `public.users` 행이 자동으로 생긴다
- Bob이 Alice의 메모·일기·근거 링크를 **한 건도 볼 수 없다**
- 사용자가 `deletions`에 쓰지 못하고 읽기만 된다
- 정책을 지우면 격리 테스트가 깨진다(뮤테이션 점검)
- 미들웨어가 요청만으로 `auth.users` 행을 만들지 않는다
- `npm run check`·`npm run test:integration`·`supabase db reset` 모두 통과

## 이번 범위 밖

- 설정 화면·연결 안내 UI (프론트 작업)
- 소셜 provider 실제 연동 (개발자 앱 등록 선행)
- 회원 탈퇴 UI와 삭제 파이프라인 워커
- 익명 계정 정리 정책·CAPTCHA (공개 배포 시점)
- 임베딩 테이블 RLS (테이블이 아직 없다)
